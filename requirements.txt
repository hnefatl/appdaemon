# On Windows, install by opening a powershell terminal from vscode (pointing to
# remote filesystem) then run
#
#   python -m pip install -r .\dev-requirements.txt

phue~=1.1
typing-extensions~=4.12
requests~=2.32
whenever~=0.6.9